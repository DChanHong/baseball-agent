# 8주차 AI Agent Observability

## 프로젝트 링크

- Repository: https://github.com/DChanHong/baseball-agent
- 7주차 제출 README: https://github.com/DChanHong/baseball-agent/blob/main/README.md

## 구현한 Observability

- 사용한 방식: LangSmith managed tracing
- trace 저장 위치: LangSmith project `kbo-game-day-agent`
- trace 단위: `/chat` 요청 1건을 LangSmith trace 1건으로 기록
- agent mode: `langchain_agent_executor`
- prompt version: `kbo-game-day-agent-v1`
- chat model: `gemini-2.5-flash`
- embedding model: `text-embedding-3-small`

기록하는 항목:

| 영역 | 항목 |
|------|------|
| Request | 사용자 입력, session id, trace id, 후보 경기 수 |
| Prompt | prompt version |
| Model | chat model, embedding model |
| Tool | tool name, arguments, result, error |
| Agent Step | intermediate steps, action log, observation |
| Output | final answer |
| Context | conversation history, candidate games |

## Agent 실행 흐름

- Agent 이름: KBO 직관 가이드 Agent
- 주요 기능: KBO 경기 일정 조회, 구장 정보 조회, 날씨 조회, 좌석 지식 검색, 좌석 후보 점수화
- 종료 조건: 필요한 tool 호출 후 최종 답변 생성

주요 Tool:

| Tool | 역할 |
|------|------|
| `find_kbo_game` | 날짜와 팀 조건에 맞는 KBO 경기 조회 |
| `get_stadium_info` | 구장 위치, 돔 여부, 홈팀, 날씨 좌표 조회 |
| `get_weather_context` | 경기일/경기시간 기준 날씨 context 생성 |
| `search_baseball_knowledge` | 좌석 추천에 필요한 구장/좌석 지식 검색 |
| `score_seat_candidates` | 선호도, 날씨, 좌석 후보를 기준으로 추천 좌석 점수화 |

## 정상 케이스 Trace 1: 다음주 롯데 경기 일정 조회

입력:

```text
다음주 롯데 경기 알려줘
```

이전 대화 context:

```text
없음
```

Trace 정보:

| 항목 | 값 |
|------|----|
| LangSmith project | `kbo-game-day-agent` |
| LangSmith public link | https://smith.langchain.com/public/4bc4aaf8-8881-48bd-949b-b06d94ad36cf/r |
| trace id | `kbo_c213dd10440947f1828489e01e62f741` |
| session id | `server-951352c0be324701966586829f366157` |
| agent mode | `langchain_agent_executor` |
| chat model | `gemini-2.5-flash` |
| embedding model | `text-embedding-3-small` |

실행 요약:

| Step | Type | Name | 주요 입력 | 결과 |
|------|------|------|-----------|------|
| 1 | tool_call | `find_kbo_game` | `team_query=롯데`, `date=다음주` | `status=ambiguous_game`, 롯데 후보 경기 6개 반환 |

Tool 결과 상세:

| 날짜 | 요일 | 시간 | 경기 | 구장 | 홈/원정 |
|------|------|------|------|------|---------|
| 2026-05-26 | 화 | 18:30 | 롯데 vs LG | 부산 사직야구장 | 홈 |
| 2026-05-27 | 수 | 18:30 | 롯데 vs LG | 부산 사직야구장 | 홈 |
| 2026-05-28 | 목 | 18:30 | 롯데 vs LG | 부산 사직야구장 | 홈 |
| 2026-05-29 | 금 | 18:30 | 롯데 vs NC | 창원 NC파크 | 원정 |
| 2026-05-30 | 토 | 17:00 | 롯데 vs NC | 창원 NC파크 | 원정 |
| 2026-05-31 | 일 | 14:00 | 롯데 vs NC | 창원 NC파크 | 원정 |

최종 답변:

```text
다음주 롯데 자이언츠 경기 6개를 날짜, 요일, 시간, 구장, 홈/원정 정보와 함께 안내했다.
후보 경기가 여러 개이므로 어떤 경기를 더 자세히 알고 싶은지 추가 선택을 요청했다.
```

분석:

- 일정 조회 요청에서는 `find_kbo_game`만 호출하고, 구장/날씨/좌석 추천 tool은 호출하지 않았다.
- `status=ambiguous_game`은 오류가 아니라 후보 경기가 여러 개인 정상 상태로 처리됐다.
- 최종 답변은 tool 결과의 후보 6개를 벗어나지 않고 그대로 요약했다.
- metadata의 `candidate_game_count`가 `0`으로 기록되어 있지만 tool observation에는 후보 6개가 반환됐다. trace metadata 집계 값과 tool 결과가 불일치하므로 metadata 계산 로직을 점검할 필요가 있다.

## 정상 케이스 Trace 2: 사직 롯데 경기 좌석 추천

입력:

```text
2026년 5월 23일 롯데 경기 좌석 추천해줘. 가성비 좋고 응 원하기 좋은 자리로 알려줘
```

이전 대화 context:

```text
사용자: 다음주 롯데 경기 알려줘
Agent: 2026년 5월 26일~31일 롯데 후보 경기 6개를 안내하고 추가 선택을 요청함
```

Trace 정보:

| 항목 | 값 |
|------|----|
| LangSmith project | `kbo-game-day-agent` |
| LangSmith public link | https://smith.langchain.com/public/97dc0af0-3363-48b9-82a7-cbc56ad7a037/r |
| trace id | `kbo_c99b376b248d485fa30af26e0a1c6521` |
| session id | `server-951352c0be324701966586829f366157` |
| revision id | `940ebd7-dirty` |
| candidate game count | 6 |
| selected game id | `null` |
| selected stadium id | `null` |

실행 요약:

| Step | Type | Name | 주요 입력 | 결과 |
|------|------|------|-----------|------|
| 1 | tool_call | `find_kbo_game` | `date=2026-05-23`, `team_query=롯데` | `status=found`, `2026-05-23` 토요일 17:00 사직 롯데-삼성 경기 확정 |
| 2 | tool_call | `get_stadium_info` | `stadium_id=sajik` | 부산 사직야구장 정보 반환, 비돔 구장, 좌표 `35.194, 129.0615`, 날씨 grid `nx=98`, `ny=76` |
| 3 | tool_call | `get_weather_context` | `stadium_id=sajik`, `game_date=2026-05-23`, `game_time=17:00`, `is_dome=false` | `status=weather_based`, 대체로 맑음, 기온 19.5도, 강수확률 8%, 예상 강수량 0mm |
| 4 | tool_call | `search_baseball_knowledge` | `query=사직야구장 가성비 좋고 응원하기 좋은 좌석`, `purpose=seat_recommendation` | 좌석 후보 문서 4개 반환 |
| 5 | tool_call | `score_seat_candidates` | 경기 정보, 날씨 context, 좌석 후보, `preferences=가성비 좋고 응원하기 좋은 자리`, `cheering_team=롯데` | 좌석 추천 후보 3개 점수화 |

Tool 결과 상세:

| 항목 | 요약 |
|------|------|
| 경기 | `2026-05-23-삼성-롯데-사직`, 롯데 자이언츠 홈, 삼성 라이온즈 원정 |
| 구장 | 부산 사직야구장, 비돔, 수용 인원 22,758명, 롯데 자체 예매 |
| 날씨 | 17:00 기준 대체로 맑음, 19.5도, 강수확률 8%, 강수량 0mm |
| 검색된 좌석 | 중앙상단탁자석(시야방해), 네이버 클립존(응원탁자석), 1루내야상단석(초등생), 1루내야상단석(청소년) |
| 추천 기준 | 가성비, 롯데 응원, 홈팀 좌석 데이터, 날씨 risk 없음 |

최종 답변:

```text
2026년 5월 23일 토요일 17:00 부산 사직야구장에서 열리는 롯데 자이언츠 vs 삼성 라이온즈 경기를 확인했다.
날씨는 대체로 맑고 19.5도로 쾌적하다고 설명했다.
가성비와 응원 분위기를 기준으로 중앙상단탁자석(시야방해)과 1루내야상단석을 추천했다.
좌석/가격 데이터는 크롤링 시점 기준이며 실시간 잔여석은 예매 시 다시 확인해야 한다고 안내했다.
```

## 실패 또는 예외 케이스 Trace

입력:

```text
2026년 2월 1일 롯데 좌석 추천해줘
```

이전 대화 context:

```text
사용자: 다음주 롯데 경기 알려줘
Agent: 2026년 5월 26일~31일 롯데 후보 경기 6개를 안내함
사용자: 2026년 5월 23일 롯데 경기 좌석 추천해줘
Agent: 2026년 5월 23일 사직 롯데-삼성 경기 좌석을 추천함
```

Trace 정보:

| 항목 | 값 |
|------|----|
| LangSmith project | `kbo-game-day-agent` |
| LangSmith public link | https://smith.langchain.com/public/b729ae8a-2bf9-499b-9454-07e8e3e5c504/r |
| trace id | `kbo_1864690d1a6445bbbca434c7772ddb5c` |
| session id | `server-951352c0be324701966586829f366157` |
| revision id | `940ebd7-dirty` |
| selected game id | `2026-05-23-삼성-롯데-사직` |
| selected stadium id | `sajik` |

실행 요약:

| Step | Type | Name | 주요 입력 | 결과 |
|------|------|------|-----------|------|
| 1 | tool_call | `find_kbo_game` | `date=2026년 2월 1일`, `team_query=롯데` | `ok=false`, `status=not_found`, `error.code=GAME_NOT_FOUND` |

Tool 결과 상세:

| 항목 | 값 |
|------|----|
| status | `not_found` |
| ok | `false` |
| error code | `GAME_NOT_FOUND` |
| error message | 조건에 맞는 KBO 경기를 찾지 못했습니다. |
| data | `null` |

최종 답변:

```text
2026년 2월 1일에는 KBO 롯데 경기가 없습니다. KBO 정규 시즌은 보통 3월 말에서 4월 초에 시작합니다.
```

실패 처리:

- 경기 조회 단계에서 실패했기 때문에 `get_stadium_info`, `get_weather_context`, `search_baseball_knowledge`, `score_seat_candidates`는 호출하지 않았다.
- 이전 대화 context에는 `selected_game_id=2026-05-23-삼성-롯데-사직`과 `selected_stadium_id=sajik`이 남아 있었지만, 새 입력의 명시 날짜 `2026년 2월 1일`을 우선하여 새 경기 조회를 수행했다.
- Agent 자체는 예외로 중단되지 않고, 사용자가 이해할 수 있는 fallback 답변을 생성했다.

## Trace 분석

- 예상한 흐름: 일정 조회는 `find_kbo_game`만 호출하고, 좌석 추천은 날짜와 팀으로 경기를 먼저 확정한 뒤 구장 정보와 날씨를 조회하고 좌석 지식 검색 결과를 점수화해야 한다.
- 실제 흐름: 일정 조회 trace는 `find_kbo_game` 1회로 종료됐다. 좌석 추천 trace는 `find_kbo_game` -> `get_stadium_info` -> `get_weather_context` -> `search_baseball_knowledge` -> `score_seat_candidates` 순서로 실행됐다. 실패 trace는 `find_kbo_game`에서 `GAME_NOT_FOUND`를 반환한 뒤 후속 tool 호출 없이 종료됐다.
- 잘 동작한 부분: 후보 경기가 여러 개인 일정 조회에서는 후보 목록을 보여주고 추가 선택을 요청했다. 좌석 추천에서는 바로 RAG 검색을 하지 않고, 먼저 실제 경기와 구장을 확정했다. 실패 trace에서는 이전 선택 경기 context가 남아 있어도 새 입력의 날짜를 우선하여 잘못된 좌석 추천으로 이어지지 않았다.
- 문제 또는 개선할 부분: 일정 조회 trace에서 metadata의 `candidate_game_count=0`과 tool observation의 후보 6개가 불일치했다. metadata 집계 위치가 tool 결과 반영 전인지 확인해야 한다.
- 문제 또는 개선할 부분: 실패 trace metadata에는 이전 대화의 `candidate_game_count=1`, `selected_game_id`, `selected_stadium_id`가 남아 있다. 최종 동작에는 영향이 없었지만, trace 분석 시 현재 요청의 상태와 이전 context 상태를 구분할 수 있도록 metadata naming을 개선하는 것이 좋다.
- 문제 또는 개선할 부분: `score_seat_candidates` 결과에서 `1루내야상단석`이 초등생/청소년 요금 문서로 중복 추천됐다. 같은 좌석명은 audience나 가격 옵션을 합쳐 하나의 추천 항목으로 병합하는 후처리가 필요하다.
- 문제 또는 개선할 부분: `중앙상단탁자석 (시야방해)`은 가성비와 탁자 편의성은 좋지만 시야방해가 있으므로, 최종 답변에서 장점보다 주의사항을 더 명확히 강조하는 것이 좋다.
- 문제 또는 개선할 부분: LangSmith export에 모델 signature와 일부 깨진 텍스트가 포함되어 제출용 문서에서는 제외했다.

## Metrics

이번 LangSmith export에서 step별 latency와 total latency 값은 별도 숫자로 확인되지 않았다. 대신 관찰 가능한 실행 지표는 아래와 같다.

| 항목 | 일정 조회 Trace 1 | 좌석 추천 Trace 2 | 실패 Trace | 설명 |
|------|------------------|------------------|------------|------|
| total latency | LangSmith 화면에서 확인 필요 | LangSmith 화면에서 확인 필요 | LangSmith 화면에서 확인 필요 | 제공된 export에는 숫자 없음 |
| step count | 1 | 5 | 1 | tool call 수 |
| tool error count | 0 | 0 | 1 | 실패 trace에서 `GAME_NOT_FOUND` |
| candidate game count | 6 | 1 | 0 | 실패 trace는 현재 요청 기준 후보 없음 |
| retrieved document count | 0 | 4 | 0 | 좌석 추천에서만 RAG 좌석 문서 검색 |
| recommendation count | 0 | 3 | 0 | 좌석 추천에서만 점수화 결과 생성 |

## 민감정보 처리

- 저장하지 않은 정보: API key, `.env`, LangSmith API key, 결제 정보, 연락처, 주소는 제출 문서에 포함하지 않는다.
- masking한 정보: LangSmith `signature`, function-call thought signature, 긴 base64 형태의 모델 내부 metadata는 제출 문서에서 제외했다.
- trace 공유 시 주의할 점: `session_id`, `trace_id`는 실행 재현과 추적을 위해 남겼지만, 사용자 개인정보가 포함되지 않은 값인지 확인했다.
- trace 공유 시 주의할 점: 외부 공유용 trace에는 사용자 개인정보, 주소, 결제 정보, 연락처가 포함되지 않도록 한다.
- 이번 trace에는 야구 경기 추천 예시만 포함되어 개인정보성 입력은 없다.

## 고도화 평가

| 평가 항목 | 구현 여부 | 결과 |
|-----------|-----------|------|
| correctness | 미구현 | trace 수동 분석으로 대체 |
| groundedness | 미구현 | 최종 답변이 tool observation의 경기, 날씨, 좌석 후보에 근거하는지 수동 확인 |
| tool completeness | 미구현 | 정상 trace에서 필요한 tool 5개가 순서대로 호출됐는지 수동 확인 |

## 배운 점

- 좌석 추천처럼 여러 정보를 조합하는 Agent는 최종 답변만으로는 판단 근거를 확인하기 어렵다.
- LangSmith trace를 보면 단순 일정 조회와 좌석 추천의 tool 호출 범위가 다르다는 점을 확인할 수 있다.
- 좌석 추천 trace에서는 경기 조회, 구장 조회, 날씨 조회, 좌석 검색, 점수화가 어떤 순서로 실행됐는지 바로 확인할 수 있다.
- 실패 trace에서는 이전 context가 남아 있어도 새 입력의 명시 조건을 우선하는지 확인할 수 있었다.
- 정상 trace에서도 중복 좌석 추천, 시야방해 좌석의 주의사항 강조 부족 같은 개선 지점을 찾을 수 있었다.
